import { ButtonforupdatePipe } from './buttonforupdate.pipe';

describe('ButtonforupdatePipe', () => {
  it('create an instance', () => {
    const pipe = new ButtonforupdatePipe();
    expect(pipe).toBeTruthy();
  });
});
